# === OS / Editor ===
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
.zed/
.fleet/
.phpactor.json

# === Logs / Cache ===
*.log
.phpunit.result.cache
.phpunit.cache/
.phpunit.cache/**

# === Laravel env files ===
.env
.env.*
!.env.example

# === Auth / Secrets ===
auth.json

# === Dependencies ===
/vendor/
/node_modules/

# === Build artifacts ===
/public/build/
/public/hot

# === Storage (uploaded files, logs, cache, sessions, etc.) ===
/storage/
/storage/**

# Re-allow the folder itself if needed (optional)
!storage/.gitignore

# === Public storage symlink ===
/public/storage

# === Laravel caches (IMPORTANT for your Dusk issue) ===
/bootstrap/cache/*.php
!/bootstrap/cache/.gitignore

# === Laravel Sail / Homestead ===
Homestead.json
Homestead.yaml

# === Testing artifacts / reports (optional but recommended) ===
/coverage/
/storage/logs/*.xml
/storage/logs/*.json
/storage/logs/*.log
